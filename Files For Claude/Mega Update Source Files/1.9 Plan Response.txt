A couple of notes to confirm here before moving forward. Please ask questions about each response so I know we are on the same page.

General Notes
*I want to install the superpowers skill

*I want this version we first build to be 1.9 and then after testing and what ever tweaks we add the next version will be 2.0

*I forgot to say one thing on the Harmless setup. If it's possible, since this is being made for people who don't know how to make music, could this have a toggle in the top left corner for a basic and an advanced mode. I would like the advanced mode to be all of the features in full but maybe if we could have the basic mode be a little bit more user friendly by just giving them the basic stuff they need to make sounds with the synth. Also important to note that presets would need to save this data for both basic and advanced. If we can't do a basic setup then that leads into my next point.

*Add to the end of the plan to review the user manual, how patches, presets and templates are made and saved, the preset plan and a new template plan and lastly a general framework plan for making other applications based on what we've learned and done.

*Anything that was deferred in this response to a later date or in the lower priority items should all be added to an action list for review after this plan is completed.

Existing Assets
*SynthVoice / WavetableOscillator / FMOscillatorKeep 
 *If we can't do a basic setup, or if it would be easier to do it this way while not having to remove this feature,  then can we use this to create a basic synth setup that is also listed on the layers page dropdown as BaySickSynth. This would have the components that are currently on that synth setup and would be addressed in the v1.9 testing and tweaks.

*MasteringEngine
 *Please see MasteringPage below

*MasteringPage
 *I want to change this, since the effects page will be setup with a drop down to select which channel you are affecting to have the Master channel in the effects page dropdown. This change will require one more effect being added to the effects pool called Tape which can be found at "C:\Users\jeffm\Documents\Vibesynth\Files For Claude\Mega Update Source Files\Effects\tape.txt". Since the master will now have it's own selection on the dropdown on the effects page, and since all of the effects are being changed to function in a rack configuration, lets let the user fill this out.

*ParametricEQDisplay
 *This is listed as being reused as the EQ sub tab inside each page, but to confirm per 4C there will be no EQ tab on the drums page. 
 *In addition I want to add this same setup to the effects page where it has two tabs, one for the effects rack page and one for an EQ page with the EQ page being apart of the effects rack chain. 
  *The logic is when you change the dropdown on the effects page to another channel that channels effects rack page tab and the eq page tab are specific for that channel and that every channel has this setup.
  *I also want the EQ on the effects page only (not the ones on the layers or bass page tabs) to have an option to have separate EQ band settings for Mid and Side so that they can swap between the two and see two different saved states for mid and side.

*SequencerPage
 *This page can be removed in full as long as it's not removing any components used by anything else as the piano roll will be the only way to sequence notes in VibeDAW

1F
*Lists the Mega.nz links but you told me I can't use those in my installer and need direct https links. Unfortunately Google Drive Direct Link isn't letting me make them so are the mega links not usable at all? Please advise.

2B
*As stated in ParametricEQDisplay I want a 2 tab system here as well with the first tab being the effects rack and the second tab being an EQ with mid/side capability which will only affect the EQ on the effects page.
*I also would like the ability to move the effects up and down on the rack affecting where in the chain they are. I want this to be able to be done by up and down arrow buttons on the left.

4B
*To confirm the bass page should also function like the layers page in that:
 *Multiple Bass tabs can coexist (each owns independent processor instance)

4C
*Lets also add a limit on having just 1 drum page tab max at a time. If the user creates one and then deletes it they can create another but they are blocked from creating a second one if one already exists.

4D
*Will this also build instruments based on the .SFZ patch files

5B 
*I listed above in MasteringPage and MasteringEngine changes that I want but I still want "MasteringEngine wrapped as a graph node between BussNode sum and hardware output" this to be true, just the behavioral change in terms of where it sits in the UI.

5C
*To reduce load on the system lets allow up to 100 tracks and only load tracks in as they are actively being used by one of the page tabs. This way we aren't running every channel no matter if it's being used or not and reducing load on the system as well as restricting to 100 tracks. Lets also make sure that the mixer can scroll horizontally so that the channels aren't getting compacted. 
*To confirm for the FX master switch, does this reduce system load when turned off? Also if turned off I want to make sure that if effects were placed on the effects rack for that channel that saves that state.

6A
*Since the undo manager is gonna have a lot more to work with now lets add a setting in general under options in the title bar menu to select the amount of undo history that is stored with the options of 100/250/500/1000. This way the user can determine what works best for them and their computer setup.

6B
*You list audio clips for use on the builder page but currently there is no way to do that. Lets let users drag and drop items from a local folder directly into the builder page OR on the File menu add an option to "Import Audio" with the allowed filed types being .wav and .mp3

Critical Missing Items
*1.MIDI Input/Output device configuration
 *Yes, v2.0 supports live MIDI input, but it does so automatically. There is no specific MIDI device picker because the software is designed to listen to 'Omni' (all active MIDI devices recognized by your operating system).
 *As long as your external controller is plugged in before launching the app, any MIDI notes you send will automatically be routed to the currently selected instrument page. We will update the Options menu and our documentation to explicitly state this so future users aren't left searching!"

*2.Hardware Audio Device Setup (ASIO)
 *Yes I want a full ASIO dialog

*3.Plugin Hosting (VST/AU third-party effects)
 *There is no plan to host VST's at this time nor for v2.1 but we don't want to rule out the possibility in the future.

*4.Browser Panel
 *Yes please build a minimal read only project browser that shows patterns and their automation targets.

*5.Undo History Window
 *Yes please build the undo history window

*6.Time Signature Support
 *Yes please add this to patterdata

*7.Audio Recording
 *Yes lets hold off on this feature for a later date

*8.Sample library URLs are Mega.nz folder links
 *I have installed MEGAcmd, what do I need to do to set this up with the links I already have? Or will this need to be uploaded differently through MEGAcmd?

*9.Project Auto-Save / Crash Recovery
 *Please implement an autosave feature with the logic that the project auto saves every 15 minutes, does that seem like a reasonable amount of time?

*10.Font & Asset Bundling
 *Yes please assure uniformity across the UI and it's font





