Role: You are an expert systems architect and specialist in Digital Audio Workstation (DAW) signal flow and mixer logic.

Task: Document and implement a dynamic signal routing architecture based on the following specifications.

1. Global Routing Hierarchy (Fixed)
Channel 0: Master Channel (Final Output)

Channel 1: Layers Buss → Master Channel

Channel 2: Bass Buss → Master Channel

Channel 3: Drums Buss → Master Channel

2. Dynamic Mixer Channel Allocation
The mixer must dynamically sort and insert channels based on active pages. Use the following logic:

Sorting Order: [Layers Group] → [Bass Group] → [Drums Group].

Grouping: Each active group must have a visual "break" (empty channel/spacer) at the beginning and end.

Drums Logic:

If ONLY the Drum page exists: Drums occupy Channels 4–17.

If Layers or Bass pages exist: They are inserted before the Drum channels, shifting the Drum channel start index accordingly.

3. Individual Signal Pathing
Follow these exact routing chains for every generated instance:

Drums Page: 14 Drum Selection Dropdowns → 14 Respective Piano Roll Keys → 14 Respective Mixer Channels → 14 Respective Effects Pages → Drums Buss (Ch 3) → Master Channel (Ch 0).

Layers Page (Per Tab): Layers Page Sound Tab → Layers Page Piano Roll Tab → Layers Page EQ Tab → Inserted Mixer Channel → Channel’s Effects Page → Layers Buss (Ch 1) → Master Channel (Ch 0).

Bass Page (Per Tab): Bass Page Sound Tab → Bass Page Piano Roll Tab → Bass Page EQ Tab → Inserted Mixer Channel → Channel’s Effects Page → Bass Buss (Ch 2) → Master Channel (Ch 0).

Objective: Please confirm you understand this logic. 

Why this works for your DAW setup:
The "Logic Gate": By explicitly telling Claude that Layers/Bass "shift" the drum index, you prevent it from hard-coding the drums to Channel 4 if those other pages aren't present.

Visual Organization: Mentioning the "breaks" ensures the AI accounts for those empty mixer tracks in its index calculations.

Standardization: It forces the AI to route everything through the Busses (1, 2, or 3) before hitting the Master, maintaining a clean sub-mixing workflow.