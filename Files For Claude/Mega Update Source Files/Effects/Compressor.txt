Here is the prompt for the Compressor module.

I noticed the text file includes specific instructions about FL Studio's mixer routing for the sidechain. Since you are building this for your own custom DAW's effect rack, I have adapted the prompt so Claude writes the C++ class to accept an external sidechain audio buffer directly into the process method. This gives your audio engine total control over how sidechain routing is handled.

Copy and paste this to Claude:

Act as an expert C++/JUCE audio DSP developer. I am building a custom DAW and need a raw C++ DSP class for a Compressor effect with sidechain support. Do NOT use juce::AudioProcessor, juce::AudioProcessorValueTreeState, or any UI/Editor code. I only need a decoupled DSP block (e.g., class CompressorDSP) that I can instantiate and chain inside my own custom audio engine.

1. Class Architecture
Create a class called CompressorDSP that follows the standard juce::dsp paradigm. It should have:

A prepare(const juce::dsp::ProcessSpec& spec) method to initialize envelopes, filters, and sample rates.

A process method that takes the main audio block, and an optional sidechain audio block. For example: process(const juce::dsp::ProcessContextReplacing<float>& context, const juce::AudioBuffer<float>* sidechainBuffer = nullptr).

A reset() method to clear envelope follower states.

A Parameters struct or dedicated setter methods for all the variables below.

2. Parameters
Threshold: Float from 0.0 to -60.0 dB. Sets the level at which compression kicks in.

Ratio: Float from 0.4 to 30.0 (representing 0.4:1 to 30:1). Controls the amount of gain reduction. Note that values below 1:1 should act as upward expansion.

Gain: Float from -30.0 to +30.0 dB. Make-up gain applied after compression.

Attack: Float from 0.0 to 400.0 ms. Time to reach full compression.

Release: Float from 1.0 to 4000.0 ms. Time to stop acting after the level falls below threshold.

Type (Knee): Choice (Hard, Medium, Vintage, Soft, Hard/R, Medium/R, Vintage/R, Soft/R).

Hard means compression takes place immediately at the threshold.

Soft/Medium/Vintage dictate different curve widths and program-dependent behaviors around the threshold. Ensure the math translates these into an actual knee width (in dB) for the gain computer.

3. DSP Signal Flow (process method)
You can utilize juce::dsp::Compressor as a starting point, but you will likely need a custom gain computer and envelope follower to support the specific Ratio expansion (< 1:1), the various Knee types, and the external sidechain.

Sidechain Logic:

If a sidechainBuffer is provided, calculate the detector signal level (RMS or Peak) from the sidechain buffer instead of the main input buffer.

If no sidechain buffer is provided, use the main input buffer for the detector.

Envelope Follower: Apply the Attack and Release times to smooth the detector signal.

Gain Computer:

Calculate the necessary gain reduction based on the Threshold, Ratio, and the selected Knee Type.

Ensure ratios below 1.0 properly apply upward expansion math.

Application & Make-up Gain: Apply the calculated gain reduction to the main audio signal, then apply the final make-up Gain (in dB).

4. Metering Variables
Include a public getter method or an std::atomic<float> variable so my engine can poll it for the UI:

getCurrentGainReduction(): Returns the current amount of gain reduction being applied (in dB), smoothed appropriately for visual metering.

Please provide the complete .h and .cpp implementation for the CompressorDSP class.