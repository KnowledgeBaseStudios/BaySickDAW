Act as an expert C++/JUCE audio DSP developer. I am building a custom DAW and need a raw C++ DSP class for a highly complex, feature-rich Delay effect. Do NOT use juce::AudioProcessor, juce::AudioProcessorValueTreeState, or any UI/Editor code. I only need a decoupled DSP block (e.g., class DelayDSP) that I can instantiate and chain inside my own custom audio engine.

1. Class Architecture
Create a class called DelayDSP following the juce::dsp paradigm. It should have:

A prepare(const juce::dsp::ProcessSpec& spec) method.

A process(const juce::dsp::ProcessContextReplacing<float>& context) method.

A setHostBPM(double bpm) method to allow the engine to pass the current tempo for sync calculations.

A reset() method to clear all delay lines and filter states.

A Parameters struct with getter/setter methods.

2. Parameters
Group the parameters logically in the struct:

Input: Wet In (Input level scaling).

Delay Time: Time (1ms to 1000ms, or musical subdivisions if synced), Tempo Sync (Bool), Keep Pitch (Bool - determines if delay line reads use interpolation that causes pitch-shifting vs crossfading), Smoothing (slew rate for time changes), Offset/Pan (stereo offset percentage).

Delay Model: Choice (Mono, Stereo, Ping-Pong, Off), Stereo Spread (0.0 to 1.0).

Feedback: Level (0.0 to >1.0 for self-oscillation), Filter Type (LP, HP, BP, Off), Cutoff, Resonance.

Lo-Fi (in feedback loop): Samp Rate (downsampling target in Hz), Bits (bitcrushing depth).

Modulation: Rate (0-20 Hz sine wave LFO), Time Mod (amount applied to Delay Time), Cutoff Mod (amount applied to Feedback Filter Cutoff).

Diffusion: Level (amount of smearing), Spread (time spread of the smear).

Feedback Distortion: Type (Limit, Saturation), Knee (soft vs hard), Symmetry (symmetrical vs asymmetrical clipping), Level (threshold for distortion).

Output: Tone (Bipolar: negative is LP, positive is HP), Wet Out, Dry Out.

3. DSP Signal Flow (process method)
This effect requires a highly specific internal routing topology, specifically within the feedback loop.

Input Stage: Apply Wet In gain to the incoming audio.

Delay Core: Implement a stereo juce::dsp::DelayLine.

If Keep Pitch is OFF, time changes should smoothly pitch-shift (standard interpolation). If ON, time changes should crossfade to the new tap to avoid pitch artifacts.

Apply Offset/Pan and handle routing based on the Delay Model (Mono sums to center, Stereo keeps separate lines, Ping-Pong swaps L/R buffers on feedback).

LFO Modulation: Generate a sine wave LFO. Apply its instantaneous value to modulate the delay read pointers (Time Mod) and the feedback filter cutoff (Cutoff Mod).

The Feedback Loop (Per Sample or Small Block): Before writing the delayed signal back into the delay line, process it through this exact chain:

Filter: juce::dsp::StateVariableTPTFilter modulated by the LFO.

Lo-Fi: Apply decimation (downsampling) and bit quantization.

Distortion: Apply the custom Waveshaper based on the Type, Knee, Symmetry, and Level parameters.

Gain: Multiply by the Feedback Level parameter.

Diffusion: Pass the delayed signal through a network of all-pass filters (like a mini-reverb) controlled by Diffusion Level and Spread.

Output Stage:

Apply the Tone filter (1-pole LP or HP depending on the knob's bipolar position) to the wet signal.

Sum the scaled Dry Out and the final processed Wet Out.

4. Smooth Parameter Updates
Because delay time changes can cause clicks, ensure that the Smoothing parameter is used to apply a juce::SmoothedValue or slew limiter to the delay time parameter, so it ramps smoothly to new targets.

Please provide the complete .h and .cpp implementation for the DelayDSP class, heavily utilizing standard JUCE DSP modules where appropriate, but writing custom DSP math for the Bitcrusher and the Asymmetrical Waveshaper.