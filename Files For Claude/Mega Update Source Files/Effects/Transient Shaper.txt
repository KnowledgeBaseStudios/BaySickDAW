Act as an expert C++/JUCE audio DSP developer. I am building a custom DAW and need a raw C++ DSP class for a frequency-split Transient Shaper effect. Do NOT use juce::AudioProcessor, juce::AudioProcessorValueTreeState, or any UI/Editor code. I only need a decoupled DSP block (e.g., class TransientShaperDSP) that I can instantiate and chain inside my own custom audio engine.

1. Class Architecture
Create a class called TransientShaperDSP that follows the standard juce::dsp paradigm. It should have:

A prepare(const juce::dsp::ProcessSpec& spec) method to initialize crossover filters and envelope followers.

A process(const juce::dsp::ProcessContextReplacing<float>& context) method for the audio loop.

A reset() method to clear filter states and envelope histories.

A Parameters struct or dedicated setter methods for all the variables below.

2. Parameters
Bypass: Boolean. Switches the effect completely out of the signal chain.

Split Frequency: Float from 5.0 Hz to 1000.0 Hz (Default 260 Hz). The crossover point separating the unprocessed Low band from the processed High band.

Split Balance: Float from -100.0% to 100.0%. Controls the mix between the bands. 0% is normal summing. -100% outputs only the unprocessed Low band. 100% outputs only the processed High band.

Attack: Float from -100.0% (cut) to +100.0% (boost). Adjusts the gain of the detected transient peaks.

Attack Shape: Enum/Choice (Sharp, Medium, Soft). Modifies the window size/time constants of the attack envelope detector (Sharp = fast/narrow, Soft = slow/wide).

Release (Sustain): Float from -100.0% (cut) to +100.0% (boost). Adjusts the gain of the decay portion of the signal.

Release Shape: Enum/Choice (Sharp, Medium, Soft). Modifies the window size/time constants of the release envelope detector.

Drive: Float. Applies analog-style saturation (rounding/distorting peaks) to the summed signal.

Gain: Float (in dB). Overall output level compensation.

3. DSP Signal Flow (process method)
Implement a split-band architecture using juce::dsp::LinkwitzRileyFilter or juce::dsp::StateVariableTPTFilter:

Crossover Split: Split the input signal into a Low-Pass band and a High-Pass band at the Split Frequency. Note: The Low-Pass band is never processed by the transient algorithm.

Transient Detection (High-Pass Band Only):

Implement an envelope extraction algorithm (typically utilizing a fast peak follower and a slower RMS/peak follower to isolate the transient difference).

Map the Attack Shape and Release Shape to specific time-constants (ms) for these envelope followers.

Transient Processing:

Calculate dynamic gain multipliers for the attack and release phases based on the Attack and Release parameters.

Apply these gain multipliers to the High-Pass audio band.

Summing & Balance:

Scale the unprocessed Low band and the processed High band according to the Split Balance parameter logic, then sum them back together.

Drive & Output:

Pass the summed signal through a soft-clipper/waveshaper driven by the Drive parameter. Calculate if clipping/saturation actually occurs during the block so the UI can light an LED later.

Apply the final overall Gain adjustment.

4. Metering Variables
Include getter methods or std::atomic<float> variables so my engine can poll them for the UI:

getCurrentInputLevel(): Peak or RMS of the input block.

getCurrentOutputLevel(): Peak or RMS of the final output block.

getSaturationAmount(): A float (or boolean) indicating how much the Drive waveshaper is actively distorting the signal, to drive an LED indicator.

Please provide the complete .h and .cpp implementation for the TransientShaperDSP class.