Act as an expert C++ DSP audio engineer. We are currently building a custom Digital Audio Workstation (DAW) and need to implement a new native multi-effect DSP module that accurately emulates a physical Type 1 Cassette Tape recorder. 

Please generate the C++ header and implementation files for this DSP effect rack. The architecture must be modular, efficient, and suitable for real-time audio processing in a standard DAW environment. 

Here are the strict DSP specifications for the signal chain, which must be processed in this exact chronological order:

1. Input & Saturation Stage:
- Implement a non-linear waveshaper with +/- 24dB Drive.
- The saturation algorithm must include embedded hysteresis to simulate magnetic tape memory (the current output should depend slightly on past inputs).
- Include 4x oversampling for this specific block to prevent digital aliasing of the generated harmonics.

2. Impulse Response (IR) Convolution:
- Implement a convolution engine block to load and apply a short impulse response (IR) of a Type 1 cassette. This will handle the static EQ curve, resonances, and phase shifts.
- Provide a method to load the IR from an external `.wav` file or memory block.

3. Degradation Filtering:
- Implement a Low Pass Filter sweepable from 5kHz to 22kHz.
- Use a standard 12dB/octave or 24dB/octave slope with no resonance.

4. Modulation Engine (Wow & Flutter):
- Implement a modulated delay line using fractional delay interpolation (e.g., Hermite or Cubic) to prevent zipper noise and artifacts.
- Wow: A slow LFO (0.5Hz - 2Hz sine wave) modulating the delay time to simulate motor drift. Depth 0-100%.
- Flutter: A fast LFO (10Hz - 20Hz, optionally with smoothed random noise) modulating the delay time to simulate capstan friction. Depth 0-100%.

5. Noise Injection:
- Implement a parallel continuous audio sampler that plays back a pre-recorded tape hiss `.wav` file.
- Mix this static noise floor into the signal just before the final output, with an adjustable level from -80dB to 0dB.

Please provide the code in two parts:
1. The `.h` header file defining the classes, parameters, and block structures.
2. The `.cpp` implementation file containing the `processBlock` logic and DSP math. 

Ensure the code is heavily commented, optimized for low CPU usage, and avoids allocating memory on the audio thread.