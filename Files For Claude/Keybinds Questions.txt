## Pre-flight question

The spreadsheet mixes pure key shortcuts with **mouse-modifier interactions** (e.g. "Alt+Right-Click", "Ctrl+Left-click", "Middle-Mouse-Click"). JUCE's `KeyMappingEditorComponent` only handles pure key shortcuts — mouse-modifiers live in component `mouseDown/mouseDrag` code and aren't user-editable from the same UI. **Are you OK with mouse-modifier rows being implemented as fixed (non-editable) and listed in the popup as "(mouse)" entries for documentation only?** Same question applies to "Mouse-wheel" / "Shift+Mouse-wheel" rows. 

This is fine and really just for reference of functionality

---

## Conflicts (current behavior differs from spreadsheet)

### General sheet
1. **F2 (Rename pattern)** — Spreadsheet says "Open name editor for selected pattern". Currently F2 in Builder renames the **track row** (the per-row label in the arrangement grid). Question: does the spreadsheet's "selected pattern" mean (a) the global current pattern in the transport-bar dropdown, or (b) a Builder grid block? If (a), what should F2 do when no pattern is selected anywhere — open the dropdown's current pattern's rename?

Option A as this applies to the active pattern from the pattern drop down on the transit bar which dictates which pattern is being played back, and there is always atleast 1 pattern that is active so the second part isn't an issue


2. **F4 (New Pattern if none)** — Currently F4 in Builder = "find next empty bar on selected row". Spreadsheet wants F4 = "create new pattern if none exists". Drop the Builder-grid behavior, or rebind it to a different key?

Drop builder behavior as that is covered in your next question


3. **F5–F8 mapping** — Current menu labels claim F5=Layers, F6=Bass, F7=Drums, F8=Builder (handlers aren't wired anyway). Spreadsheet wants F5=Mixer, F6=Effects, F7=Builder, F8=Layers, F9=Bass, F10=Drums, F11=Piano Roll. Confirm the spreadsheet wins and the menu labels get updated.

Spreadsheet wins

4. **F11 (Piano Roll most recent)** — There's no standalone "Piano Roll" page; piano rolls are sub-tabs inside Layers/Bass/Drums. Should F11 jump to the **last-used** piano-roll sub-tab on whichever Layers/Bass/Drums tab was last active? (Mirrors how F8/F9/F10 already work via "Most Recent".) 

Yes last used piano roll sub tab but can we include for drums that the drum kit tab counts as a piano roll as well if that's the last piano roll they were on?


5. **Ctrl+P (Recording metronome precount)** — Currently Ctrl+P inside Builder = Import Audio. Spreadsheet wants Ctrl+P = toggle precount metronome. Also note: Builder sheet says Ctrl+P = Toggle Performance Mode, which is a third meaning. Confirm which of the three should win on Builder; what should Import Audio be rebound to (Ctrl+I? menu only?).

Metronome wins as the other two have other means of doing this


### Builder sheet
6. **Z (Zoom tool)** — Currently Shift+Z in Builder. Spreadsheet wants bare Z. Drop Shift+Z, or keep both?

Yes just Z


7. **PgUp / PgDown** — Currently scroll one viewport-page in Builder grid. Spreadsheet wants zoom in/out. Confirm replace.

This is just in reference to zoom tool being active otherwise it would still scroll normally


### Piano Roll sheet
8. **Z (Zoom tool)** — Currently Shift+Z in piano roll. Same question as #6.

Yes Just Z

9. **S vs O (Note-type shortcuts)** — Currently bare **S** cycles Standard → Slide → Portamento with one keystroke. Spreadsheet splits: `S` = Slide toggle, `O` = Portamento toggle (and adds them as separate states, not a 3-way cycle). Should we keep the cycle, or split into independent toggles? If split: when both are off → Standard; when only Slide on → Slide; when only Portamento on → Portamento; when both on → ? (this combination is currently impossible).

Split 3 buttons and only one of the 3 can be active at a time. That being said the slide and portamento doesn't seem to do anything as the play note isn't impacted by the slide or portamento note. Also only one of them has a symbol (I'm assuming the arrow is slide) and both slide and portamento need a symbol showing that they are different.


10. **Alt+G vs Shift+Alt+G (Ungroup)** — Currently Shift+G groups, Shift+Alt+G ungroups. Spreadsheet wants Shift+G group / Alt+G ungroup. Drop the Shift+ prefix on ungroup?

Spreadsheet wins

11. **Alt+L vs Ctrl+L** — Currently Alt+L = Articulate (articulation tool). Spreadsheet wants Ctrl+L = Quick legato. These are similar but distinct concepts. Is "Articulate" the same as "Quick Legato" (rename), or different features (need to keep Alt+L and add Ctrl+L)? Keep alt+L for articulate and ctrl+l for quick legato 

---

## New features — need implementation explanation

These don't exist yet. I need a sentence or two on what each should actually do before I can wire a shortcut to nothing.

### Pattern navigation
12. **+ / - (Next/Previous pattern)** — Cycle through `mPatternManager` patterns by index? What happens past the last (wrap, or stop)? Does this only work outside the piano roll (since `+/-` collide with most other contexts)?

I want it to wrap and yes this doesn't work on piano roll due to colliding contexts



13. **F3 (Next empty pattern)** — Move the **transport-bar pattern dropdown** to the first pattern that has zero notes/blocks, or create one if none exist?

Exactly this



### Transport / playhead
14. **L (Switch Pattern/Song mode)** — Toggle the transport-bar Pattern↔Song button. Confirm.

confirm

15. **NumPad 0 (Fast forward)** — Hold-to-FF, or single press jumps N bars? FL's behavior is "skip forward 4 bars per press".

Skip 4 bars per press

16. **NumPad / and \* (Previous/Next bar in Song mode)** — Move playhead by ±1 bar? Only when transport is in Song mode (no-op in Pattern mode), or always? 

move playhead 1 bar and only in song mode



17. **Home (Move playback marker to start)** — In Builder grid, or globally for the transport playhead? Both?


Both in song and pattern so globally



### Metronome / typing keyboard
18. **Ctrl+T (Toggle typing keypad to piano keypad)** — There's no "typing-keyboard plays MIDI" feature yet. Want me to add it? If yes: which keys → which notes (FL uses the QWERTY row as white keys with a 2-octave range)?

Yes I want you to add this and yes use QWERTY for 2-octave range

19. **Ctrl+M (Toggle metronome)** — Toggle the metronome on/off. The metronome panel already exists. Confirm just a toggle.

confirmed toggle

20. **Ctrl+P (Recording metronome precount)** — Need clarification: is this a 1/2/4-bar precount that runs *only when recording is armed*, played out the metronome before the actual recording starts? (See conflict #5 too.)

So this feature already exists but it was setup before arming record was setup. It currently allows 1/2/4 bar but that is wrong and it should be how you described it something that runs when recording is armed played before the recording start and it should just be 1 bar always or 4 beats as the lead in.



### Builder — features that don't exist
21. **Alt+M / Alt+Shift+M (Mute / Unmute selection)** — Per-block mute flag for arrangement blocks? Currently no per-block mute. Add to `ArrangementBlock`?

Yes per block mute flag


22. **Alt+T (Add Time marker)** — No time-marker concept exists. Add a `TimeMarker { bar, label }` list to `PatternManager`, with a draw/edit UI in Builder?


Yes and it should have a way to change the label of the marker with maybe hover showing the tooltip for the name and right click allowing an edit name feature. Also lets make it so you can right click on the ruler to set a time marker as well.



23. **Shift+Alt+T (Set Time Signature at playhead)** — Time signature is currently project-global. Spreadsheet implies per-bar TS changes. Add per-bar TS list?


Yes add this capability as well and lets add this to the right click ruler to set options as well.


24. **Right-Alt+Right-Click (Quantize selected)** — What's the quantize unit for arrangement blocks (snap to bar? to step?)?


This should bring up a pop up menu that gives you different options for quantize such as the unit and anything else that should be there


25. **Right-Alt+Left-click (Mute selected)** — Same as #21 (mute flag).

Yes this mutes the selected note or notes

26. **Ctrl+Shift+Right-Click (Zoom to selected clip)** — Zoom Builder grid so the selected block fills the viewport?

correct


27. **Ctrl+Right-Click (Zoom on drag)** — Drag-to-define rectangle, release to zoom-fit?


Correct


28. **Performance Mode (Ctrl+P, Builder)** — No Performance Mode exists. What is it? (FL has a live-trigger mode for clips — is that what you mean?)


This is what I mean



### Piano Roll — features that don't exist
29. **M (Keyboard view mode toggle)** — Toggle the piano-roll keyboard column visibility (hide it for more horizontal space)?

Correct


30. **Alt+E (Riff machine)** — A pattern-generator dialog (chord progressions / arpeggios)? FL's Riff Machine generates random rhythms within a scale. Want me to spec one as a separate session, or stub for now?

I want a riff machine built yes



31. **Alt+F (Flam)** — Add quick flam pairs (a soft grace note before each selected note)?

Correct

32. **Alt+V (Ghost Channels toggle)** — "Ghost notes" = notes from other tabs displayed dimmed. Already partially exists (display only). Want a toggle to show/hide them?

Ghost notes toggled on and off, also ghost notes still needs to be addressed as a whole as this wasn't fixed with the drums like it was supposed to be


33. **Alt+X (Scale Levels tool)** — FL's note-velocity-curve drawing tool. Spec separately?

This should bring up a pop up window that lets you set with a slider and/or text box the velocity for the selected notes and if multiple notes are selected it affects all of the selected notes



34. **Alt+Mouse-wheel (Change note property under cursor)** — Which property cycles? (FL's default is velocity.)

These are the options in the control lane drop down so the default you are referring to is correct



35. **Alt+NumPad 2–6 (Change typing-keyboard octave)** — Same prerequisite as #18 (typing-keyboard piano feature).

Yes this goes along with the typing keyboard


36. **Ctrl+M (Import MIDI)** — Conflicts with #19 globally (toggle metronome). Confirm: is `Ctrl+M` global metronome or piano-roll MIDI import? FL uses Alt+I for MIDI import.


Lets change this to Alt+I then




37. **Ctrl+Q (Quick Quantize)** — Different from Alt+Q's tool dialog? FL's "Quick Quantize" snaps to the current grid setting with no dialog.

I want the FL feature where it snaps to the closest 1/4 note without a dialog box


38. **Ctrl+U (Quick Chop)** — Same as #37 — chop into 4 with no dialog?

This is correct, did we not have an alt+U as well for a chop dialog box? If so add that in as well

39. **Ctrl+L (Quick legato)** — See conflict #11. If different from Articulate, what's the spec? FL's "Quick Legato" extends each note to the start of the next.


Do what FL Studio does

40. **Ctrl+Alt+Home (Allow resizing from left toggle)** — A mode where note left-edge becomes draggable?

Correct



41. **Ctrl+Delete (Delete space equal to selection)** — Time-shift all notes after the selection back by selection-length?

Correct


42. **Ctrl+Up/Down-Arrow (Transpose octave)** — Transpose selection ±12 semitones?

Correct

43. **Ctrl+Left/Right-Arrow (Select time before / after)** — Marquee-style time selection extension?

This is based on the selection already made so if I selected on the ruler 2 bars and hit these keys to move right it would my selection box only, not the contents in it, over to the next 2 bars

44. **Shift+G (Group)** — Already wired. ✓


45. **Shift+N (Turn off selected chord)** — What's a "chord" here? Does the piano roll have chord groupings? "Turn off" = mute, or remove?

I thought that a chord placed would delete as a group but they are individual blocks so this can be removed.


46. **Shift+Ctrl+V (Paste from MIDI clipboard)** — Different from regular paste — a SMF blob from external apps?

Persistant between piano rolls on different pages

47. **Double Left-click on note** — Note-properties dialog (velocity, pan, length, type)?

Yes just like how we have setup on the drum kit that you can double left click a note to change the midi note I want this function on all piano roll notes everywhere to include what you stated and any other relevant data


48. **Double Right-click on ghost note** — "Swap to ghost channel to edit it" — switch the active tab to the tab that owns the ghost note?

correct


### Export
49. **Ctrl+R (Export)** — Currently you have Export submenu with "Export as WAV" / "Export as MP3". Should Ctrl+R open the WAV export dialog directly, or open a chooser?


these two shouldn't be separate sub menu items and should be one Export Audio that goes to a pop up window with export options. Also the export doesn't even currently function and needs to be addressed.

