# QA-ManualPress: authored control blurbs, merged into the generated Controls
# tables by generate-manual.py.  Keys are the control's param id (from
# bsd-docs.json); controls with no id key on "<figure name>|<label>".
# Register rules (batch plan "shutter-snapping-shrike"): verb-first, 3-9
# words, effect first, no taglines.  Authored in Tasks 5-7.
BLURBS = {}
