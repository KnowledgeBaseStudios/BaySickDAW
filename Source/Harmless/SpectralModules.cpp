#include "SpectralModules.h"

// All implementations are inline in the header (each module is small).
// This translation unit exists so CMake has a .cpp to compile for the
// SpectralModules object - prevents "no object files" linker warnings.
