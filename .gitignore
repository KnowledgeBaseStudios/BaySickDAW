# Build output — regenerated by do_build.bat every time, no reason to track.
build/

# Claude / planning working notes — local-only.
Files For Claude/

# User-created projects + their backups + samples — runtime data, not source.
Projects/
